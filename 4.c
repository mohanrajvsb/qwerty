#include <stdio.h>
void main()

{    int n1,m =20,n=40;
    clrscr();
    printf("Print Odd Numbers in a given range m to n:\n");
    for (num = m; n1 <= n; num++)
        {
            if (n % 2 == 1)
              printf ("%d ", n1);
        }
   getch();

}
