#include <stdio.h>
 void main()
{
    int i, n, sum = 0;
    printf("Enter number \n");
    scanf ("%d", &num);
    for (i = 1; i <= n; i++)
    {
        sum = sum + i;
    }
    printf ("Sum of first %d natural numbers = %d\n", n, sum);
}
